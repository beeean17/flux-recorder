from __future__ import annotations

from typing import Literal

AppMode = Literal["webcam", "screen", "convert"]

WEBCAM_MODE: AppMode = "webcam"
SCREEN_MODE: AppMode = "screen"
CONVERT_MODE: AppMode = "convert"
